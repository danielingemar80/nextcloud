{{/*
=============================================================================
tenant-secret.yaml — NO SECRETS ARE CREATED BY THIS CHART.

All secrets are managed externally (Vault, SealedSecrets, ESO, etc.)
and referenced by name+key in values.yaml under each credentials block.

Expected secrets per tenant (example for "acme"):
  Secret name: nextcloud-acme-env
  Keys:
    NEXTCLOUD_ADMIN_PASSWORD   → Nextcloud admin password
    DB_PASSWORD                → Database password
    OBJECTSTORE_KEY            → S3 / Ceph RGW access key
    OBJECTSTORE_SECRET         → S3 / Ceph RGW secret key

Expected secret for Redis:
  Secret name: nextcloud-redis-env
  Keys:
    REDIS_PASSWORD             → Redis AUTH password

You can override the secret name and key names per-tenant in values.yaml:
  nextcloud.credentials.name / adminPasswordKey
  database.credentials.name  / passwordKey
  s3.credentials.name        / accessKeyKey / secretAccessKey
  redis.credentials.name     / passwordKey
=============================================================================
*/}}
{{/* Intentionally empty — secrets are external */}}
